server:
  port: 8902

spring:
  application:
    name: hystrix-turbine

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

turbine:
  app-config: hystrix-turbine
  cluster-name-expression: "default"
  combine-host-port: true
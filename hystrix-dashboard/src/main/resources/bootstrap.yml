server:
  port: 8901
spring:
  application:
    name: hystrix-dashboard
  boot:
    admin:
      client:
        url: http://localhost:8900
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
management:
  security:
    enabled: false

server:
  port: 8030
spring:
  application:
    ### 服务名称
    name: order-service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

feign:
  hystrix:
    enabled: true
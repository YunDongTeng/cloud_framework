spring:
  profiles:
    active: dev
  zipkin:
    base-url: http://localhost:9411
hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: true
        isolation:
          thread:
            timeoutInMilliseconds: 9000

ribbon:
  ReadTimeout: 3000
  ConnectTimeout: 3000
logging:
  config: classpath:logback-spring.xml
